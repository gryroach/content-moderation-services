# 🐳 Docker-инфраструктура проекта

> Руководство по запуску и управлению Docker-контейнерами проекта.

## 📑 Содержание

- [Структура](#структура)
  - [Docker-файлы](#docker-файлы)
  - [Сети](#сети-docker)
  - [Тома](#тома-данных)
- [Запуск](#запуск)
  - [Быстрый старт](#быстрый-старт)
  - [Пошаговый запуск](#пошаговый-запуск)
- [Управление сервисами](#управление-сервисами)
  - [Все сервисы](#управление-всеми-сервисами)
  - [Основные сервисы](#управление-основными-сервисами)
  - [Сервисы модерации](#управление-сервисами-модерации)
  - [MongoDB](#управление-mongodb)
  - [Kafka](#управление-kafka)
- [Отладка](#отладка)
  - [MongoDB](#отладка-mongodb)
  - [Kafka](#отладка-kafka)
  - [Сети](#отладка-сетевых-проблем)

## Структура

### Docker-файлы

Проект разделен на несколько docker-compose файлов для модульности:

| Файл                            | Описание                              |
| ------------------------------- | ------------------------------------- |
| `docker-compose.yml`            | Основные сервисы (nginx, ugc-api)     |
| `docker-compose.mongodb.yml`    | Шардированная MongoDB с репликацией   |
| `docker-compose.kafka.yml`      | Kafka-кластер и UI для мониторинга    |
| `docker-compose.moderation.yml` | Сервисы модерации (API, БД, frontend) |

Такое разделение позволяет запускать только нужные компоненты и упрощает разработку.

### Сети Docker

В проекте используется единая сеть `graduate_work_network`, которая объединяет все сервисы:

| Сеть                    | Назначение                           |
| ----------------------- | ------------------------------------ |
| `graduate_work_network` | Общая сеть для всех сервисов проекта |

Эта архитектура позволяет:

- Упростить взаимодействие между сервисами
- Обеспечить единое пространство имен для всех контейнеров
- Устранить проблемы с порядком запуска и зависимостями между сетями
- Облегчить мониторинг и отладку

Сеть создается автоматически при запуске любого из сервисов через Makefile команды или скрипты запуска.

#### Автоматическое управление сетями

В проекте реализован автоматический механизм инициализации сети `graduate_work_network` со всеми необходимыми метками:

1. **Автоматическое создание сети перед запуском сервисов**

   - Все команды `make` для запуска контейнеров автоматически инициализируют сеть перед выполнением
   - Используется механизм "декораторов" в Makefile, который прозрачно вызывает `init-networks.sh`
   - Разработчик может просто вызывать команды запуска, не беспокоясь о сетях

2. **Проверка и восстановление меток сети**

   - Скрипт `init-networks.sh` проверяет наличие корректных меток Docker Compose
   - При необходимости, сеть пересоздается с правильными метками
   - Это предотвращает ошибки типа "network exists but was not created by compose"

3. **Единая конфигурация сети во всех docker-compose файлах**
   - Во всех файлах docker-compose.\*.yml используется параметр `external: true`
   - Это указывает Docker Compose использовать существующую сеть, а не создавать новую

Этот подход позволяет надежно запускать микросервисы по отдельности без проблем с сетями.

### Тома данных

Для хранения данных используются именованные Docker-тома:

- MongoDB: тома для данных шардов и конфиг-серверов
- PostgreSQL: том для данных модерации
- Kafka: тома для данных брокеров

## Запуск

### Быстрый старт

Для просмотра всех доступных команд:

```bash
make help
```

Для запуска всего проекта:

```bash
make run-all
```

### Пошаговый запуск

Для корректной работы рекомендуется следующий порядок запуска:

1. Запустите MongoDB и инициализируйте её:

   ```bash
   make run-mongo
   make init-db
   make create-dbuser
   ```

2. Запустите Kafka:

   ```bash
   make run-kafka
   ```

3. Запустите основные сервисы:

   ```bash
   make run-core
   ```

4. Запустите сервисы модерации:
   ```bash
   make run-moderation
   ```

## Управление сервисами

### Управление всеми сервисами

```bash
# Запуск всех сервисов
make run-all

# Остановка всех сервисов
make down-all

# Просмотр статуса всех сервисов
make ps-all

# Просмотр логов всех сервисов
make logs-all
```

### Управление основными сервисами

```bash
# Запуск основных сервисов
make run-core

# Остановка основных сервисов
make down-core
```

### Управление сервисами модерации

```bash
# Запуск всех сервисов модерации
make run-moderation

# Запуск только API и БД модерации
make run-moderation-api

# Запуск frontend в режиме разработки
make run-moderation-frontend

# Запуск frontend в production режиме
make run-moderation-frontend-prod

# Остановка всех сервисов модерации
make down-moderation

# Остановка только frontend
make down-moderation-frontend

# Просмотр логов модерации
make logs-moderation

# Просмотр статуса модерации
make ps-moderation
```

### Управление MongoDB

```bash
# Запуск MongoDB
make run-mongo

# Остановка MongoDB
make down-mongo

# Просмотр статуса MongoDB
make ps-mongo

# Просмотр логов MongoDB
make logs-mongo

# Инициализация шардов
make init-db

# Создание пользователя
make create-dbuser
```

### Управление Kafka

```bash
# Запуск Kafka
make run-kafka

# Остановка Kafka
make down-kafka

# Просмотр статуса Kafka
make ps-kafka

# Просмотр логов Kafka
make logs-kafka
```

## Отладка

### Отладка MongoDB

Для подключения к MongoDB и проверки статуса шардов:

```bash
# Подключение к mongos
docker exec -it mongos1 mongosh

# В консоли mongosh выполните:
sh.status()
```

### Отладка Kafka

Для мониторинга Kafka:

1. Откройте Kafka UI в браузере: http://localhost:8080
2. Проверьте статус брокеров и топиков
3. Для просмотра детальных логов:
   ```bash
   make logs-kafka
   ```

### Отладка сетевых проблем

При проблемах с сетевым взаимодействием:

```bash
# Проверка существования основной сети
docker network ls | grep graduate_work_network

# Проверка подключения контейнера к сети
docker inspect -f '{{range $key, $value := .NetworkSettings.Networks}}{{$key}} {{end}}' <имя_контейнера>

# Проверка доступности других контейнеров
docker exec -it <имя_контейнера> ping <имя_другого_контейнера>
```

#### Использование скрипта инициализации сетей

Для ручного управления сетью `graduate_work_network` используйте скрипт `init-networks.sh`:

```bash
# Создание/проверка сети с правильными метками
./init-networks.sh

# Удаление сети (если нет активных контейнеров)
./init-networks.sh clean
```

Если вы столкнулись с ошибкой типа `network exists but was not created by compose`, выполните:

```bash
# Остановка всех сервисов
make down-all

# Удаление и пересоздание сети
./init-networks.sh clean
./init-networks.sh

# Запуск нужных сервисов
make run-moderation-api  # или любую другую команду запуска
```

#### Проверка меток сети

Для проверки правильности меток Docker Compose:

```bash
# Проверка меток сети
docker network inspect graduate_work_network | grep -A 3 Labels

# Ожидаемые метки:
# "com.docker.compose.network": "graduate_work_network"
# "com.docker.compose.project": "graduate_work"
```

При разработке новых сервисов, убедитесь, что в их `docker-compose.yml` указан параметр `external: true` для сети:

```yaml
networks:
  graduate_work_network:
    name: graduate_work_network
    external: true
```

## Дополнительная информация

- Основные сервисы настроены с использованием переменных окружения из файла `.env` в корне проекта
- Frontend сервис модерации использует файл переменных окружения `moderation_frontend/.env`
- MongoDB настроена с шардированием и репликацией для высокой доступности
- Kafka использует KRaft (без Zookeeper) и настроена с репликацией для отказоустойчивости
- Для мониторинга Kafka доступен веб-интерфейс Kafka UI (http://localhost:8080)
- Все сервисы используют единую сеть Docker `graduate_work_network` для упрощения взаимодействия между компонентами
